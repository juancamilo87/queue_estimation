<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aware.plugin.queuetracescollector">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="ANDROID.PERMISSION.WRITE_EXTERNAL_STORAGE" />

    <application android:allowBackup="true" android:label="@string/app_name"

        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme"
        tools:replace="android:icon, android:theme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCTHF8szeeuNorL5ZBOOAEkrUySviuQgSU"/>

        <service android:name="com.aware.plugin.queuetracescollector.Plugin" android:exported="true" />
        <activity android:name="com.aware.plugin.queuetracescollector.Settings" android:exported="true" android:theme="@style/Theme.Aware" />

        <activity
            android:name="com.aware.plugin.queuetracescollector.HelperActivity"
            android:exported="true"
            android:theme="@style/Theme.Aware">
        </activity>

        <receiver android:name="com.aware.plugin.queuetracescollector.NotificationReceiver">
            <intent-filter>
                <action android:name="com.aware.plugin.queuetracescollector.notification"/>
            </intent-filter>
        </receiver>

        <provider
            android:name="com.aware.plugin.queuetracescollector.Provider"
            android:authorities="${applicationId}.provider.queuetracescollector"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.aware.READ_CONTEXT_DATA"
            android:writePermission="com.aware.WRITE_CONTEXT_DATA">
        </provider>

        <activity
            android:name="com.aware.plugin.queuetracescollector.app.HomeScreen"
            android:label="@string/title_activity_home_screen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.aware.plugin.queuetracescollector.app.ChoosePlaceActivity"
            android:label="@string/title_activity_choose_place" >
        </activity>
        <activity
            android:name="com.aware.plugin.queuetracescollector.app.MessagesActivity"
            android:label="@string/title_activity_messages"
            android:windowSoftInputMode="stateHidden|adjustResize"
            >
        </activity>

        <activity
            android:name="com.aware.plugin.queuetracescollector.app.HeatMap"
            android:label="Heat map"
            android:windowSoftInputMode="stateHidden|adjustResize"
            >
        </activity>

    </application>

</manifest>
