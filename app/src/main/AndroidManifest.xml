<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aware.plugin.tracescollector">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <application android:allowBackup="true" android:label="@string/app_name"

        android:icon="@drawable/ic_launcher"
        android:theme="@style/Theme.Aware">

        <service android:name="com.aware.plugin.tracescollector.Plugin" android:exported="true" />
        <activity android:name="com.aware.plugin.tracescollector.Settings" android:exported="true" android:theme="@style/Theme.Aware" />

        <activity
            android:name="com.aware.plugin.tracescollector.HelperActivity"
            android:exported="true"
            android:theme="@style/Theme.Aware">
        </activity>

        <receiver android:name="com.aware.plugin.tracescollector.NotificationReceiver">
            <intent-filter>
                <action android:name="com.aware.plugin.tracescollector.notification"/>
            </intent-filter>
        </receiver>

        <provider
            android:name="com.aware.plugin.tracescollector.Provider"
            android:authorities="${applicationId}.provider.tracescollector"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.aware.READ_CONTEXT_DATA"
            android:writePermission="com.aware.WRITE_CONTEXT_DATA">
        </provider>

    </application>

</manifest>
